@model IEnumerable<LaCatalanaWeb.Models.EstadoUsuario>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create">Nuevo Estado Usuario</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
